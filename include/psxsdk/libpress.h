#ifndef PSXSDK_LIBPRESS_H
#define PSXSDK_LIBPRESS_H

#include "common.h"

/** @brief Reset the MDEC decoder; @p mode 0 also reinstalls the reset callback. */
extern void DecDCTReset(s32 mode);

#endif /* PSXSDK_LIBPRESS_H */
